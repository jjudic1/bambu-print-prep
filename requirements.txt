# Mesh pipeline (Phase B). Pinned loosely for now; pin hard before any deploy.
numpy>=2.0
scipy>=1.13
trimesh>=4.4
networkx>=3.3
manifold3d>=2.5
pymeshfix>=0.17
lxml>=5.2          # trimesh needs it to read .3mf input
scikit-image>=0.24   # trimesh marching cubes, for the voxel-remesh fallback
embreex>=4.0         # fast ray casting for wall-thickness sampling
rtree>=1.3
shapely>=2.0        # cross-section areas for the orientation stress test
fast-simplification>=0.1  # decimation for the orientation solver proxy mesh
pytest>=8.0
pillow>=10.0        # TESTS ONLY -- prep/render.py writes PNGs with zlib
                    # on purpose, so the worker container needs no imaging lib
